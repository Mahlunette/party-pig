<h1> <%=@party.name%> </h1>
<ol>
  <% @party.buckets.each do |bucket| %>
    <li>
      <%= bucket.name %>
    </li>
  <% end %>
</ol>

<%# Add a bucket %>

  <%= simple_form_for [@party, @bucket], remote: true do |f| %>
   <%= f.input :name %>
   <%= f.submit %>
  <% end %>
